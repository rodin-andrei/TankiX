using SharpCompress.Common;

namespace SharpCompress.Archive
{
	public class AbstractWritableArchive<TEntry, TVolume> : AbstractArchive<TEntry, TVolume>
	{
		internal AbstractWritableArchive(ArchiveType type)
		{
		}

	}
}
