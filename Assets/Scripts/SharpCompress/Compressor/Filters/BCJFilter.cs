using System.IO;

namespace SharpCompress.Compressor.Filters
{
	public class BCJFilter : Filter
	{
		public BCJFilter(bool isEncoder, Stream baseStream) : base(default(bool), default(Stream), default(int))
		{
		}

	}
}
