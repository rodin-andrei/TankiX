using System.IO;

namespace SharpCompress.Common
{
	public class GenericVolume : Volume
	{
		public GenericVolume(Stream stream, Options options) : base(default(Stream), default(Options))
		{
		}

	}
}
