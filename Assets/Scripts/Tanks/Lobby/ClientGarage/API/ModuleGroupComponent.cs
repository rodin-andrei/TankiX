using Platform.Kernel.ECS.ClientEntitySystem.API;

namespace Tanks.Lobby.ClientGarage.API
{
	public class ModuleGroupComponent : GroupComponent
	{
		public ModuleGroupComponent(Entity keyEntity) : base(default(Entity))
		{
		}

	}
}
