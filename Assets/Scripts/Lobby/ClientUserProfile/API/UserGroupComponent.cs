using Platform.Kernel.ECS.ClientEntitySystem.API;

namespace Lobby.ClientUserProfile.API
{
	public class UserGroupComponent : GroupComponent
	{
		public UserGroupComponent(Entity keyEntity) : base(default(Entity))
		{
		}

	}
}
